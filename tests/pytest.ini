[pytest]
markers =
    need_review: tests for stepik reviewers
    login_user: login user and run tests
    guest: tests without login
    smoke: marker for smoke tests
    xfail: for failed
filterwarnings =
    error
    ignore::DeprecationWarning